# Attribute groups

<!-- DO NOT EDIT - AUTOGENERATED FILE. Regenerate with `make generate-all` -->

Attribute group ids are part of this registry's public API surface: consumers
reference them by id, so renaming one is a breaking change.
{% for group in ctx.attribute_groups %}
## `{{ group.id }}`

**Stability:** {{ group.stability }}

{{ group.brief | trim }}
{%- if group.note %}

{{ group.note | trim }}
{%- endif %}
{% if group.attributes | default([]) | length > 0 %}
| Attribute | Type | Description |
|---|---|---|
{%- for attribute in group.attributes | sort(attribute="key") %}
| `{{ attribute.key }}` | `{% if attribute.type is string %}{{ attribute.type }}{% else %}enum{% endif %}` | {{ attribute.brief | trim | replace("\n", " ") }} |
{%- endfor %}
{% endif %}
{%- endfor %}
