# Entity namespace: `{{ ctx.root_namespace }}`

<!-- DO NOT EDIT - AUTOGENERATED FILE. Regenerate with `make generate-all` -->
{% for entity in ctx.entities | sort(attribute="type") %}
## `{{ entity.type }}`

**Stability:** {{ entity.stability }}

{{ entity.brief | trim }}
{%- if entity.note %}

{{ entity.note | trim }}
{%- endif %}
{% if entity.identity | default([]) | length > 0 %}
Identifying attributes:

| Attribute | Type | Description |
|---|---|---|
{%- for attribute in entity.identity | sort(attribute="key") %}
| `{{ attribute.key }}` | `{% if attribute.type is string %}{{ attribute.type }}{% else %}enum{% endif %}` | {{ attribute.brief | trim | replace("\n", " ") }} |
{%- endfor %}
{% endif %}
{%- endfor %}
