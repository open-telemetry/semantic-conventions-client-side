# Attribute namespace: `{{ ctx.root_namespace }}`

<!-- DO NOT EDIT - AUTOGENERATED FILE. Regenerate with `make generate-all` -->

| Attribute | Type | Stability | Description | Examples |
|---|---|---|---|---|
{%- for attribute in ctx.attributes | sort(attribute="key") %}
| `{{ attribute.key }}` | `{% if attribute.type is string %}{{ attribute.type }}{% else %}enum{% endif %}` | {{ attribute.stability }} | {{ attribute.brief | trim | replace("\n", " ") }} | {% if attribute.examples is defined %}{% if attribute.examples is string %}`{{ attribute.examples }}`{% else %}{% for example in attribute.examples %}`{{ example }}`{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}{% endif %} |
{%- endfor %}
{% for attribute in ctx.attributes | sort(attribute="key") %}
{%- if attribute.type is mapping %}

## `{{ attribute.key }}` values

`{{ attribute.key }}` has the following list of well-known values. If one of them applies, then the respective value MUST be used; otherwise, a custom value MAY be used.

| Value | Stability | Description |
|---|---|---|
{%- for member in attribute.type.members | sort(attribute="value") %}
| `{{ member.value }}` | {{ member.stability }} | {{ (member.brief or member.id) | trim | replace("\n", " ") }} |
{%- endfor %}
{%- endif %}
{%- endfor %}
{%- set noted = ctx.attributes | selectattr("note") | list %}
{%- if noted | length > 0 %}

## Notes
{% for attribute in noted | sort(attribute="key") %}
### `{{ attribute.key }}`

{{ attribute.note | trim }}
{% endfor %}
{%- endif %}
